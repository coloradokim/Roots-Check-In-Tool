extends layout

block stylesheets
	//- link(href="//cdnjs.cloudflare.com/ajax/libs/select2/4.0.0/css/select2.min.css" rel="stylesheet")

block content
	nav.navbar.navbar-default(role='navigation')
			.container-fluid
				.navbar-header
					button.navbar-toggle.collapsed(type='button' data-toggle='collapse' data-target='#bs-example-navbar-collapse-1')
						span.sr-only Toggle navigation
						span.icon-bar
						span.icon-bar
						span.icon-bar
					img(src="./img/roots-logo.png" style="width:190px;height:auto").navbar-brand


				#bs-example-navbar-collapse-1.collapse.navbar-collapse
					ul.nav
						ul.nav.navbar-nav.navbar-right(style="margin-top:9px")
							li
								a(href='/grove-overview') Grove Overview
							li
								a(href='/grove-calendar') Create Grove Activities
							li
								a(href='/student-tracker') Student Tracker

	.container.grove-overview
		a#zone-flex.zone(href='javascript:void(0);')
			h2 Flex

			.avatar-none
			h3
				span.now-count 0
				|  students scheduled
			.bottom-stripe
				.other-teachers
					.avatar.teach1
						img(src=zones[0]['zone-flex'][2])
					.avatar.teach2
							img(src=src=zones[0]['zone-flex'][1])
					.avatar.teach3
						img(src=zones[0]['zone-flex'][0])
				h3
					span.next-count 0
					|  students on deck
		a#zone-maker.zone(href='javascript:void(0);')
			h2 Maker
			.avatar-none
			h3
				span.now-count 0
				|  students scheduled
			.bottom-stripe
				.other-teachers
					.avatar.teach1
						img(src=zones[0]['zone-maker'][2])
					.avatar.teach2
						img(src=zones[0]['zone-maker'][1])
					.avatar.teach3
						img(src=zones[0]['zone-maker'][0])
				h3
					span.next-count 0
					|  students on deck
		a#zone-ipad.zone(href='javascript:void(0);')
			h2 iPad
			.avatar-none
			h3
				span.now-count 0
				|  students scheduled
			.bottom-stripe
				.other-teachers
					.avatar.teach1
						img(src=zones[0]['zone-ipad'][2])
					.avatar.teach2
						img(src=zones[0]['zone-ipad'][1])
					.avatar.teach3
						img(src=zones[0]['zone-ipad'][0])
				h3
					span.next-count 0
					|  students on deck
		a#zone-library.zone(href='javascript:void(0);')
			h2 Library
			h3
				span.now-count 0
				|  students scheduled
			.bottom-stripe
				.other-teachers
					.avatar.teach1
						img(src=zones[0]['zone-library'][2])
					.avatar.teach2
						img(src=zones[0]['zone-library'][1])
					.avatar.teach3
						img(src=zones[0]['zone-library'][0])
				h3
					span.next-count 0
					|  students on deck
		a#zone-writing.zone(href='javascript:void(0);')
			h2 Writing
			h3
				span.now-count 0
				|  students scheduled
			.bottom-stripe
				.other-teachers
					.avatar.teach1
						img(src=zones[0]['zone-writing'][2])
					.avatar.teach2
						img(src=zones[0]['zone-writing'][1])
					.avatar.teach3
						img(src=zones[0]['zone-writing'][0])
				h3
					span.next-count 0
					|  students on deck
block scripts
	script(src="./build/GroveOverview.js")
